spring:
  application:
    name: ai-knowledge-assistant

  datasource:
    url: jdbc:postgresql://localhost:5432/ai_knowledge_assistant
    username: postgres
    password: 1111


  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: true

server:
    port: 8081
